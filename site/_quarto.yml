project:
  type: website
  output-dir: _site
  preview:
    port: 4848
  # Generate analyst-friendly download artifacts (CSV + XLSX) alongside each
  # canonical *.parquet in data/processed/, then copy the whole folder into
  # _site/data/processed/ so download links on chart pages and the data
  # catalog actually resolve on the deployed site. The script handles both
  # phases: pre-render generates CSVs/XLSXs from parquets; post-render copies
  # the full folder into the rendered output. See scripts/prepare_downloads.py.
  pre-render:
    - python3 ../scripts/prepare_downloads.py --phase pre
  post-render:
    - python3 ../scripts/prepare_downloads.py --phase post
  # Static files that ship to the deployed site as-is. robots.txt sits
  # next to _quarto.yml; Quarto needs an explicit `resources` entry to
  # know it should land at the site root.
  resources:
    - "robots.txt"

website:
  title: "Livestock Marketing Alternatives Explorer"
  description: "An interactive, continuously-updated explorer of public USDA-AMS feeder-cattle price data for the four-corners region — built on public-domain USDA, BLS, and NASS feeds."
  site-url: "https://lma-explorer.github.io"
  repo-url: "https://github.com/lma-explorer/lma-explorer.github.io"
  repo-subdir: site
  repo-actions: [edit, source, issue]
  # Note on sitemap: Quarto auto-generates _site/sitemap.xml from every
  # rendered page when `site-url` is set (above). No explicit directive
  # is required. To verify, after `quarto render`, check that
  # _site/sitemap.xml exists and lists every page.
  back-to-top-navigation: true
  page-navigation: true
  reader-mode: false
  # Open-graph and Twitter card metadata. The 1200x630 PNG is generated
  # from site/assets/og-card.svg via scripts/render_og_card.py — edit the
  # SVG, regenerate the PNG, commit both. The image: value is the relative
  # path Quarto inlines into <meta og:image> and <meta twitter:image>.
  twitter-card:
    image: assets/og-card.png
    card-style: summary_large_image
  open-graph:
    image: assets/og-card.png

  navbar:
    background: light
    search: true
    left:
      - text: "Home"
        href: index.qmd
      - text: "Price–weight explorer"
        href: price-weight.qmd
      - text: "Weekly seasonality"
        href: seasonality.qmd
      - text: "Weekly trends"
        href: weekly-trends.qmd
      - text: "Basis"
        href: basis.qmd
      - text: "Sell-now compare"
        href: sell-now-compare.qmd
      - text: "LRP"
        href: lrp.qmd
      - text: "LRP backtest"
        href: lrp-backtest.qmd
      - text: "Methodology"
        href: methodology/index.qmd
      - text: "Glossary"
        href: glossary.qmd
      - text: "Data"
        href: data.qmd
      - text: "About"
        href: about.qmd
    right:
      - icon: github
        href: https://github.com/lma-explorer/lma-explorer.github.io
        aria-label: GitHub repository

  page-footer:
    left: |
      Built primarily on public USDA-AMS, BLS, and USDA NASS data; basis figures are derived from non-redistributed CME settle inputs (see [data licensing](https://github.com/lma-explorer/lma-explorer.github.io/blob/main/LICENSE-DATA.md)). Code MIT; content and derived data CC-BY-4.0.
    right: |
      See [GOVERNANCE](https://github.com/lma-explorer/lma-explorer.github.io/blob/main/GOVERNANCE.md) and [CITATION](https://github.com/lma-explorer/lma-explorer.github.io/blob/main/CITATION.cff).

format:
  html:
    theme:
      light: cosmo
      dark: darkly
    css: styles.css
    toc: true
    toc-depth: 3
    toc-location: right
    link-external-newwindow: true
    link-external-icon: true
    code-fold: true
    code-summary: "Show the code"
    fig-responsive: true
    email-obfuscation: references

execute:
  freeze: auto
  echo: false
  warning: false

# Author byline — Quarto emits this as <meta name="author"> in the HTML
# head of every rendered page, which LinkedIn's Post Inspector and other
# social-card validators read to populate the author field of a shared
# link preview. Placed at the top level (rather than inside `website:`)
# because that's where Quarto's per-document author key lives; setting
# it here applies as a default to all pages without per-page overrides.
author: "Ibrahima Sall"

editor: source
