# Raw API response cache — debugging artifact, not redistributable
/data/raw/
!/data/raw/.gitkeep

# Quarto build output
_site/
.quarto/
_freeze/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata
renv/library/
renv/local/
renv/cellar/
renv/staging/

# Jupyter
.ipynb_checkpoints/

# Environment + secrets
.env
.env.local
*.secret

# OS cruft
.DS_Store
Thumbs.db

# Editor state
.vscode/
.idea/
*.swp
*.swo

# Internal working docs — kept locally, not tracked in the public repo.
# Pulled out 2026-04-22 pending explicit approvals to publish.
DECISIONS_LOG.md
PHASE1_PLAN.md

# Third-party proprietary data — kept locally, never committed.
# REDACTED compilations (member-only); CME feeder-futures are proprietary.
# The public repo sources Clovis data directly from USDA-AMS (public-domain)
# and treats CME futures as link-out only. See LICENSE-DATA.md.
Data_REDACTED/
