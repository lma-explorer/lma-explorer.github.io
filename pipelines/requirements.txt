# Pipeline + site-render runtime dependencies.
# Pinned to minor versions for reproducibility; bump deliberately via PR.
# Tested on Python 3.11 (the default in the GitHub Actions runner as of 2026-04).
#
# Phase 1 keeps pipeline and site-render deps in one file. If the site-render
# surface grows (e.g., dash/shiny for Lab-subdomain tools in Phase 3), split
# into `pipelines/requirements.txt` + `site/requirements.txt`.

# Pipeline
requests>=2.31,<3
pandas>=2.2,<3
pyarrow>=15,<20
numpy>=1.26,<3       # explicit (transitively installed via pandas, but
                     # site/sell-now-compare.qmd imports it directly for
                     # numpy.percentile in the percentile-band logic)

# Site rendering (Quarto executes .qmd Python code chunks via jupyter;
# plotly renders the price-weight, seasonality, weekly-trends, basis,
# and sell-now-compare charts; matplotlib renders the one-pager.qmd
# producer-printable PDF mini-charts via Quarto's typst format).
jupyter>=1.0,<2
plotly>=5.24,<7
matplotlib>=3.8,<4   # one-pager.qmd typst PDF render path
openpyxl>=3.1,<4     # scripts/prepare_downloads.py writes XLSX next to each
                     # *.parquet for analyst-friendly downloads (Excel-readable
                     # alongside the canonical parquet)
